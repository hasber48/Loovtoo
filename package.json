{
  "name": "geo-quiz-tests",
  "version": "1.0.0",
  "description": "Playwright tests for Geo Quiz application",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:debug": "playwright test --debug",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
